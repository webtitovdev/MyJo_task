// import axios from 'axios';

import {ResponseType} from '../constants/response/types';
import {response} from '../constants/response/response';

export const getCards = async (): Promise<ResponseType> => {
  // const {data} = await axios.get(API_URL); fake api
  // return data; fake api
  return response;
};
